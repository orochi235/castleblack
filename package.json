{
  "name": "pezlie-workspace",
  "private": true,
  "workspaces": [
    "wall",
    "hosts/brick-icons",
    "hosts/demo"
  ],
  "scripts": {
    "test": "npm test --workspaces",
    "typecheck": "npm run typecheck --workspaces"
  },
  "peerDependencies": {
    "@weasel-js/labkit": ">=1.5.0"
  }
}
