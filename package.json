{
  "name": "castleblack",
  "private": true,
  "workspaces": ["wall", "hosts/brick-icons"],
  "scripts": {
    "test": "npm test --workspaces",
    "typecheck": "npm run typecheck --workspaces"
  }
}
